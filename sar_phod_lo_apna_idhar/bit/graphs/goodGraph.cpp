#include <bits/stdc++.h>
using namespace std;

int solve(vector<int> &A) {

    int n = A.size();
    for(int i = 0; i < n; ++i)
    A[i] -= 1;

    vector<bool> vis(n, false);

    for(int i = n-1; i >= 0; i--){
        
    }

}
